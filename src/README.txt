README

A aplicação é executada no ambiente do LINUX enviando como linha de comando
\.sosh apos a sua compilação. Para isso teremos antes de aceder ao directorio
do trabalho prático e fazer ‘make’ que executa o makefile e por conseguinte
compila o programa.

1-Executar a makefile -> $ make

2-Executar a aplicacao -> $ ./sosh
